#include "config.hpp"

DEBUG = true;

UDP_PORT = 3382; //default 3382, change with -u
INTERFACE_PORT = 3637; //default 3637, change with -U
FIND_INTERVAL = 1; //default 1, change with -t
SERVICES_INTERVAL = 10; //default 10, change with -T
REFRESH_TIME = 1; //default 1, change with -v
DNS_SD = false; //default faulse, change with -s
